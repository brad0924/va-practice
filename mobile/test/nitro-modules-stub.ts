/**
 * Nitro Modules 是 MMKV 底下那層 C++ 的接線。它在 Node 裡不存在，**被 import 的當場**
 * 就丟例外——不是等到有人叫它才丟。
 *
 * 而測試一次都不會用到它：`createMMKV()` 進門先問「我是不是跑在測試裡」，是的話直接切到
 * 套件自帶的假實作（資料存在記憶體裡），那條真接線碰都碰不到。它純粹是被 import 了而已。
 *
 * 所以這裡給一個空殼，讓那一行 import 過得去。**這不是在假造被測的東西**——被測的是
 * `createMmkvStorage()` 有沒有把介面對起來。真的存進手機、關掉 app 再開還在不在，
 * 是票 `04` 真機驗收那兩條在守的，自動測試碰不到那一段。
 */
export {};
