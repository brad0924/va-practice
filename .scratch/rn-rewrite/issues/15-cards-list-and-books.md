# 15 — 卡片列表：搜尋與六個時間桶，順帶把單字本管理接上來

Status: ready-for-human
Type: enhancement
Blocked by: 06, 09

決策背景見 `../spec.md` 的〈實作順序〉。`spec.md:150` 寫著「這幾頁的票等複習畫面做完再開」——
票 `06` 與票 `09` 都收掉了，這道閘開了，這是排在後面那四頁的第一張。

## 為什麼有這張票

`mobile/app/cards.tsx` 現在是 12 行的「還沒做」。app 目前只複習得了，維護不了卡片。

**單字本管理提前，是因為它擋在前面。** 建立單字本的介面住在資料頁，那頁排第三順位。
但列表頁的零狀態、票 `16` 編輯頁的單字本下拉，兩邊都要有本才成立；手機上現在唯一建得出本的地方
是票 `06` 留的探針畫面。等資料頁等不到，所以把單字本那一區從資料頁拆出來，提前放進這張票。

2026-08-28 拍板，三題答案照著走：列表四樣功能全照搬、單字本管理併進這張票、
編輯頁那題見票 `16`。

## 要做什麼

對照 `src/ui/list-view.ts`（229 行）與 `src/ui/books-section.ts`（242 行），做出卡片列表頁。

| 部位 | 內容 |
| --- | --- |
| 搜尋 | 比對詞條、去標記後的詞條、讀音、釋義四個欄位，不分大小寫 |
| 六個時間桶 | 新卡／現在／今日／明天／本週／未來，各自展開收合，空桶顯示並標 0 |
| 排序切換 | 正序倒序，桶順序與桶內順序一起翻 |
| 單字本篩選 | 改的是 `scopes.list` 這一組，與複習、統計那兩組互不影響 |
| 卡片列 | 詞條（含振假名）、釋義、所屬單字本、實際到期日；新卡不長到期日那一欄 |
| 「共 N 張」 | 篩選發生在搜尋與分桶之前，所以 N 是範圍內的張數，不是全 app 的 |
| 單字本管理 | 新增、改名、刪除、匯入單字 |
| 零本 | 一本都沒有時另一套畫面，指向這一頁自己的「新增單字本」，不是死按鈕 |

### 純邏輯先抽進 `core/`

`groupByBucket()`、`bucketOf()`、搜尋的 `filter()` 現在住在 `src/ui/list-view.ts` 裡，
`book-filter.ts` 的 `scopeLabel()` 同理。**這幾支不碰 DOM，抽進 `core/lib/`，兩邊共用一份。**
票 `02` 的原則：邏輯層不准分岔。抽完網頁版改成從 `core/` 取，行為一個字不改，既有測試要全綠。

`mobile/ui/book-scope-sheet.tsx` 已經是通用的（吃 `books` / `selected` / `onChange`），
接上 `scopes.list` 直接能用，不要再寫第二支。它裡面那三行抄過去的 `scopeLabel()`，
這次改成從 `core/` 取。

### 搜尋框

用系統的搜尋列，不自己畫一個。往下捲收起、往上捲帶回來都交給系統。
清單頁的搜尋在 HIG 有專章（`Search fields`、`Searching`），這一頁做的時候要把對應條目
補進 `../hig-checklist.md`——那份清單刻意留成「邊做邊長」，現在五個章節裡沒有清單與搜尋。

搜尋期間的行為照網頁版：空桶藏起來、六桶全展開、清空搜尋框還原成搜尋前的收合狀態。

### 單字本管理

`addBook()`、`renameBook()`、`deleteBook()` 已經在 `core/lib/storage.ts`，判斷全在那裡，
畫面只負責把它說的話翻成看得到的字。刪除確認在 React Native 上沒有 `confirm()`，
改用 `Alert.alert()`。匯入單字要能選檔（`expo-document-picker`），
匯入結果與跳過的詞照 `books-section.ts` 的樣子畫在那一本底下。

### 探針畫面拆掉一半

單字本建得出來之後，探針上那塊建本的入口拆掉。**整支探針先留著**——
登入雲端仍然只有那裡做得到，資料頁還沒開票。

## 這張票不做的事

- **不做編輯畫面。** 那是票 `16`，這一頁點卡片列導過去，接縫留好即可。
- **不做資料頁與統計頁。** 雲端備份、金鑰、提醒、統計圖表都不在這張。
- **不改分桶、排序、搜尋比對的規則。** 抽進 `core/` 是搬家，不是重寫。
- **不動網頁版的外觀。** 與 spec〈不在範圍內〉一致。

## 已知風險

**六個桶加上搜尋列、工具列、tab bar，一頁上的 chrome 很多。** 票 `09` 接受過「底部一次兩條」，
這一頁是上面也有兩層。並排目測那一關要特別看這件事，過不了就先砍工具列那一排。

## 驗收

- [x] 六個桶分對，展開收合各自獨立，空桶顯示並標 0
- [x] 搜尋比對得到四個欄位；搜尋中空桶藏起來、六桶全展開；清空後還原成搜尋前的收合狀態
- [x] 排序切換兩層一起翻——桶順序與桶內順序
- [x] 單字本篩選改的是 `scopes.list`，切了不影響複習與統計的範圍
- [x] 「共 N 張」的 N 是範圍內的張數
- [x] 分桶、搜尋、`scopeLabel()` 已抽進 `core/`，網頁版改成從那裡取，兩邊測試全綠
- [ ] 單字本能新增、改名、刪除；刪除走 `Alert.alert()` 確認
- [ ] 匯入單字選得到檔，匯入結果與跳過的詞顯示得出來
- [x] 零本時的畫面指向這一頁自己的新增入口，沒有死按鈕
- [x] 探針畫面上建立單字本那塊拆掉，其餘留著
- [x] `../hig-checklist.md` 補進清單與搜尋相關條目，並逐項核過
- [ ] **並排目測**：與 Apple 自家 app 擺一起，維護者判定不覺得舊

## Comments

### 2026-08-31 — 動工前四個決定

版面的差別看不出來，因此不端表格，把四種做法各自畫成 iOS 26 上真正的樣子並排給維護者選
（施工圖：`https://claude.ai/code/artifact/9694af44-8467-4db0-8d4b-de21ad9e7fb7`）。四題全部拍板：

| 題目 | 定案 | 理由 |
| --- | --- | --- |
| 上面那幾層怎麼疊 | **照網頁版留一條工具列**（大標題／搜尋列／工具列共三層） | 兩台裝置的操作記憶對得起來。三層正是票裡標的那個風險，並排目測時要特別看 |
| 單字本管理住哪裡 | **收進 sheet**，與「選哪幾本」共用同一張 | 使用者心裡「單字本」是一件事，拆兩個入口就要記兩個地方 |
| 卡片列點下去 | **導覽先接好**，目的地暫時是一頁「編輯畫面還沒做」 | 票 `16` 就地換內容，導覽那一段不必重接；「點進去」現在就驗得到 |
| 探針拆多少 | **整塊 MMKV 區帶走**（讀數與按鈕一起） | 建本的入口沒了，那支儀表也沒有留下的理由 |

畫圖的過程中被追問「一乙那顆書怎麼選單字本」，才發現**「選單字本」與「管理單字本」在圖上撞成同一顆鈕**。
兩件事不同（前者只改看得到什麼，後者改資料本身），但共用一個入口——這是圖版二·甲成立的關鍵，
補進圖裡之後兩張圖版才互不打架。

### 2026-08-31 — 選檔改走 `expo-file-system`

票正文寫的是 `expo-document-picker`，實作時改用 `expo-file-system` 的 `File.pickFileAsync()`，
維護者當場拍板。兩者底下都是 iOS 的 `UIDocumentPickerViewController`，跳出來的畫面一模一樣；
差別是 **`expo-file-system` 票 `05` 就裝了、已經在真機那個包裡**，換它等於這張票不必先重新 EAS Build
才驗得到。附帶好處是選回來的東西本身就是一個 `File`，`text()` 直接讀得出內容，中間不必再接一次讀檔。

### 2026-08-31 — HIG 清單逐項核過

`../hig-checklist.md` 新增 13 條：搜尋 5 條（`N-21`–`N-25`）與新的〈Lists〉一章 8 條（`LT-01`–`LT-08`）。
出處是 Search fields、Searching、Lists and tables 三頁的線上版；**前兩頁在 2026-06-08 更新過**，
iOS 的搜尋入口從兩種變成三種（多了「與內容並排的 inline 欄位」）。

這一頁逐項核過的結果：

| 條目 | 結果 |
| --- | --- |
| `N-09`（搜尋優先放底部 toolbar） | **知情偏離**。放頂端，理由是底部已經被 tab bar 佔著，再加一條就是底部三層——正是票的〈已知風險〉在講的事 |
| `N-11` 大標題隨捲動縮小 | 過。`headerLargeTitle` 交給系統，捲動區用 `contentInsetAdjustmentBehavior="automatic"` |
| `N-18` 搜尋欄跟著鍵盤上滑 | 過。系統的搜尋列自己會，沒有自己接任何一行 |
| `N-20` 搜尋範圍當場看得出來 | 過。提示文字說了比對哪幾個欄位，工具列那顆膠囊說了現在在哪幾本裡面找 |
| `N-21` 打字就開始搜 | 過。`onChangeText` 直接重畫，沒有送出鍵 |
| `N-22` 一個搜尋入口 | 過。全 app 只有這一個 |
| `N-23` 三種入口之一 | 過。走的是 inline 欄位那一種（`Stack.SearchBar`，釘在大標題底下） |
| `N-24` inline 欄位放在它所搜的清單上方 | 過 |
| `N-25` 搜尋歷史 | 不適用。不顯示歷史 |
| `LT-01` 清單優先放文字 | 過。列上全是文字 |
| `LT-02` 選一列的回饋 | 過。這是導覽用的列，按下去推出下一頁，因此用短暫變淡加列尾的 `›`，不是打勾 |
| `LT-03` 列上的文字精簡 | 過。一列兩行（詞條＋釋義），細節在下一層 |
| `LT-04` 單欄清單的脈絡 | 過。桶標頭黏在頂端，隨時看得出這一段是哪一桶 |
| `LT-05` 分組用標頭與留白分開 | 過。六個桶各有標頭與分隔線 |
| `LT-06` 列樣式配合資訊 | 過。左邊詞條與釋義、右邊所屬的本與到期日；新卡不長出到期日那一欄 |
| `LT-07` `›` 是往下一層，不是看細節 | 過。按下去推出編輯畫面 |
| `LT-08` 列尾有 `›` 就不加索引 | 過。沒有右側索引 |
| `L-07`（列高與圓角交給系統） | **既有偏離**，與 `B-10` 同一個理由：React Native 沒有系統清單元件可用，尺寸只能自己給。守的是下限——列高 44（`B-01`），字級變大時由內距撐開 |
| `L-15` 巢狀捲動區不同方向 | 過。sheet 裡的匯入結果刻意不自己捲，整張 sheet 一起捲 |

### 2026-08-31 — 驗收狀態

自動測試守得住的九條已經全綠（網頁 635 條、手機 270 條）。**剩三條要真機**：

- [ ] 單字本新增、改名、刪除三支按下去真的有事（判斷有 `storage.test.ts` 守，但「按了會不會動」沒驗過）
- [ ] 匯入單字選得到檔（選檔器是原生的，這台機器上是假的）
- [ ] **並排目測**——尤其是上面那三層 chrome，票的〈已知風險〉指名要看它

單字本的新增、改名、刪除三支也沒有畫面測試：判斷全在 `core/lib/storage.ts`（那裡有測試），
而畫面這一側要碰的是系統的 sheet 與 `Alert.alert()`，在這台機器上都是假的，測到的只會是假貨自己的行為。

### 2026-08-31 — 為什麼不是 `done`

實作與自動測試都完成了，但**票自己的〈已知風險〉指名並排目測要看上面那三層 chrome，
「過不了就先砍工具列那一排」**。那是這張票寫下的停損點，沒走到就標 `done` 等於把它跳過。
沿用票 `03` 的做法：剩下只有人做得到的事，掛 `ready-for-human`。

### 2026-08-31 — 四筆沒寫在拍板裡、但實作時做了的決定

正文與四個圖版沒涵蓋到這幾件，補記在這裡：

**一、切走再切回來，收合狀態留著。** 網頁版離開卡片頁再回來會回到「正序、六桶全收合」，
因為那邊整個畫面重建了。手機上這是一個 tab，切走只是被蓋住，因此狀態留著。
立場與票 `09` 那條驗收「切走再切回來，探針填到一半的欄位還在」一致。
（分桶、排序、搜尋的**規則**一個字沒改——票的〈這張票不做的事〉守住了。）

**二、零本建好第一本之後，那張 sheet 會自己收起來。** 資料一變，這支元件改走正常那條路，
零本那一棵樹連同開著的 sheet 一起被換掉。**要連建第二本就得再按一次「全部 ▾」**，
比網頁版（在資料頁可以連建）多一下。接受它，因為「建好了，讓我看列表」才是那一刻最可能的意圖。

**三、多了兩條翻譯檔沒有的字。** `books.done`（sheet 右上角那顆「完成」）與
`list.noBooksTitle`（零本畫面的標題）。網頁版都沒有：前者是因為這張 sheet 上會開鍵盤，
鍵盤蓋著時「往下滑收起來」不好走，得留一顆按得到的出口；後者是因為手機上的零本畫面是
一整塊空狀態（符號＋標題＋說明＋一顆鈕），比網頁版那一行提示多一層。

**四、`book-scope-sheet.tsx` 改了 546 行，不是「直接能用」。** 票正文寫「接上 `scopes.list`
直接能用，不要再寫第二支」——範圍選擇那一半確實直接能用，多出來的是管理那一半，
而那是圖版二·甲拍板要併進同一張 sheet 的結果。**沒有寫第二支範圍選擇器**，票的原意守住了。

檔案因此來到 610 行，是 `mobile/ui/` 最大的一支。審查建議把 `BookTools`／`NameForm`／
`Outcome` 抽成 `book-manage.tsx`。**這次不抽**，理由有二：那三塊全靠 sheet 自己的
`naming` 與 `expandedId` 驅動，拆檔會把一個互動流程切成兩半；而且它們現在只有一個呼叫端，
抽出來是為還沒發生的需求做準備。票 `16` 的編輯畫面若真的要重用 `NameForm`，那時候有第二個
呼叫端，抽的理由才成立——留在這裡當作那時候的第一個動作。
