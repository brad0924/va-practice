# 12 — 相依守門：讓「本機綠、CI 紅」在本機就紅

Status: done
Type: enhancement

決策背景見 `mobile/jest.config.js` 的 `moduleDirectories` 註解，那裡記著病灶。

## 為什麼有這張票

**同一類問題在這條線上炸過兩次，兩次都是本機怎麼跑都看不出來。**

找套件的規矩是**從用它的那個檔往上層走**。開發機的 repo 根自己有一份 `node_modules`，從 `mobile/` 往上走剛好被它接住；CI 那兩個工作只在 `mobile/` 裡裝套件，根目錄是空的，才會炸。**人不會自己想到要檢查一件在自己機器上永遠是綠的事。**

| 何時 | 缺的是 | 炸在哪 |
| --- | --- | --- |
| 票 `05`（`912c468`） | `@babel/runtime` | `test.yml` 的 mobile 工作，四支測試整批載不進來 |
| 票 `06`（`261b792`） | `vitest` | 同一支工作的 `tsc --noEmit`，四支新測試整批找不到型別 |

2026-08-25 維護者交代「等票 `06` 的真機驗收做完再提醒」。票 `06`、`08`、`09` 都收了，因此開這張票。

## 先講它擋不住什麼

**票 `05` 那次，這支檢查抓不到。**

`912c468` 的 commit 訊息寫得很清楚：

> 去叫 `@babel/runtime` 的不是 `core/` 的原始碼（那批一個外部套件都沒 import），是 babel 轉譯後產生的程式碼。

也就是說**原始碼裡根本沒有那一行 import**，一支掃原始碼的檢查再仔細也看不到它。兩次案發裡，這支檢查只擋得住票 `06` 那一種。

寫在最前面是為了不要把它想成「這類問題從此絕跡」。它擋的是**人寫錯 import**，不是**工具鏈偷偷帶進來的東西**。後者要擋得另想辦法，不在這張票裡。

## 要做什麼

在 `mobile/` 收一支測試，跟著 `npm test` 一起跑：走檔案樹、剝掉註解、抓出每一行的 import 來源，逐個確認那個套件真的找得到。缺的話當場紅燈，訊息要指出是哪一支檔的哪一個套件。

**剝註解不是可有可無的。** 現在 `mobile/` 底下有**六支**檔的**註解裡**寫著 `from 'vitest'`（五支測試的檔頭加 `test/vitest-shim.ts`），而 `vitest` 沒有裝在 `mobile/` 裡。不剝註解的話這支檢查上線第一天就有六條誤報。

**`require()` 也要抓，不是只抓 `import`。** 設定檔用的是 `require()`：`metro.config.js` 那一行 `require('expo/metro-config')` 只有這種寫法認得出來。只抓 `from '…'` 的話，`jest.config.js` 與 `metro.config.js` 兩支整個掃不到。

### 這幾條沒得選，照著寫就好

動工時不必再議，但也不要漏掉——`mobile/` 現在有 54 種不重複的 import 來源，這三類跳過規則就吃掉 32 種：

| 遇到什麼 | 怎麼辦 | 現有數量 |
| --- | --- | --- |
| 相對路徑（`./`、`../`） | 跳過，那是檔案不是套件 | 23 種 |
| 路徑別名 `@core/*` | 跳過。它是 `tsconfig`／`metro.config`／`jest.config` 三邊各設一次的別名，**不是套件**，查了必定紅燈 | 8 種 |
| 內建模組 `node:*` | 跳過 | 1 種（`node:path`） |
| 子路徑 | 取套件名那一段再查。scoped 取前兩段，其餘取第一段 | `react-native-screens/experimental`、`expo-router/unstable-native-tabs`、`expo/metro-config` |

剩下的 22 種就是要查的外部套件（剝掉註解後是 21 種，`vitest` 只出現在註解裡）。

## 四件待決已拍板（2026-08-28 triage）

原本的四個問題與各自的代價留在下方 Comments，這裡只寫定案。**動工時照這四條走，不要再議。**

### 一、掃描範圍：只掃 `mobile/`

`core/` 不掃。理由不是「範圍乾淨跑得快」，是實測出來的：

- `core/` 底下的外部 import 只有三種——`vitest`（20 次）、`firebase/ai`（1 次，在 `gemini-reading.ts`）、`node:fs`。前兩種都沒裝在 `mobile/` 裡，整批掃進來**現在就是 21 條誤報**。
- 而 `gemini-reading.ts` 根本不在 mobile 這條路上——沒有任何 mobile 的檔 import 得到它。那條誤報純屬白工。
- **兩次案發沒有一次是「`core/` 的原始碼 import 了 mobile 沒裝的套件」。** 票 `05` 缺的 `@babel/runtime` 是轉譯產物去叫的，掃原始碼本來就看不到（見上面〈先講它擋不住什麼〉）。

triage 時評估過第三條路——「從 `mobile/` 出發追 import 鏈，只掃真的被載到的 `core/` 檔」。它確實避得開 `firebase/ai` 那條誤報，但**今天的結果與只掃 `mobile/` 一模一樣（都是零紅燈）**，而且要多寫一個小型找檔器（副檔名、`index.ts`、循環偵測），起點還得去讀 `jest.config.js` 的 `testMatch` 才知道。更糟的是它與第四條的定案打架：追鏈會追到三支寫著 `from 'vitest'` 的 `core/` 檔（兩支 jest 直接跑的測試加 `core/test-setup.ts`），不放行 `vitest` 就是三條紅燈。**將來真需要時再升級，成本跟現在做一樣。**

### 二、比對的對象：查 `package.json`

不查 `node_modules`。每一個被 import 的套件都要自己列在 `mobile/package.json` 裡，靠別人順便帶進來的不算數。

**這代表動工時要順手補一行**：`@jest/globals` 被七支檔 import（不是票上原本寫的四支），但沒列在 `mobile/package.json`，是 `jest` 帶進來的。把它加進 `devDependencies`。除了這一條，現況其餘 20 種外部套件全部查得到。

### 三、`import type` 算

與票 `06` 的實際案發一致——那次炸的是 `tsc --noEmit`，不是執行期。

### 四、jest 的 `moduleNameMapper`：不讀，自己管一張放行名單

不去讀 `jest.config.js` 的映射表。**理由是它與驗收最後一條直接衝突**：那張表正好映射掉 `^vitest$`，讀了就等於放行 `vitest`，「重現票 `06`」那條驗收當場過不了。

在「只掃 `mobile/` + 查 `package.json`」的組合下，**這張放行名單現在是空的**——映射表那三條，`@core/*` 已由跳過規則吃掉，`react-native-nitro-modules` 本來就列在 `package.json` 裡，`vitest` 只出現在註解裡會被剝掉。名單留著是為了將來真的需要時有地方寫，不是現在就要填東西進去。

## 這張票不做的事

- **不擋工具鏈偷偷帶進來的相依**（票 `05` 那一種）。理由見上面〈先講它擋不住什麼〉。
- **不掃 `core/`，也不去讀 `jest.config.js` 的 `moduleNameMapper`。** 兩條都是拍板決定，理由見上面〈四件待決已拍板〉的第一與第四條。
- **不改 CI 設定。** 這支檢查跟著 `npm test` 走，CI 本來就會跑到。
- **不動 `moduleDirectories` 那個修法。** 那是票 `05` 的解方，仍然有效，這裡只是多加一道在本機就會響的警報。

## 驗收

- [x] `mobile/` 底下每一支 `.ts`／`.tsx`／`.js` 的 import 都被掃到，包含測試檔與設定檔
- [x] `require('…')` 也抓得到——`metro.config.js` 那行 `require('expo/metro-config')` 要被掃進來
- [x] 註解裡的 `from '…'` 不算——拿現在那六支寫著 `from 'vitest'` 的檔驗，一條誤報都不該有
- [x] 相對路徑、`@core/*` 別名、`node:*` 內建模組都不誤報
- [x] 子路徑取得出套件名：`react-native-screens/experimental` 要查 `react-native-screens`
- [x] `core/` 底下的檔沒有被掃到——那 20 條 `from 'vitest'` 與 `firebase/ai` 一條都不該冒出來
- [x] `@jest/globals` 已補進 `mobile/package.json` 的 `devDependencies`
- [x] 故意 import 一個不存在的套件，這支測試會紅，而且訊息指得出是哪支檔、哪個套件
- [x] 現況跑起來是綠的（照上面四條定案的規則，且 `@jest/globals` 已補）
- [x] 重現一次票 `06`：在 `mobile/` 隨便一支測試寫 `from 'vitest'`，這支檢查要紅

## Comments

### 2026-08-28 — triage：四件待決全數拍板，狀態轉 `ready-for-agent`

> *This was generated by AI during triage.*

**動手前在 codebase 上查掉的三件事：**

1. **沒有重複實作。** `mobile/lib/`、`mobile/test/` 底下沒有任何掃 import 的測試，`.out-of-scope/` 目錄不存在（本 repo 還沒有被拒絕過的需求紀錄）。
2. **`core/` 的外部相依實測。** 只有三種：`vitest`（20 次，全在測試檔）、`firebase/ai`（1 次，`gemini-reading.ts` 的 `import type { SchemaRequest }`）、`node:fs`（1 次，`cloud-backup.test.ts`）。前兩種在 `mobile/node_modules` 與 `mobile/package.json` 兩邊都沒有。
3. **票上兩處數字要更正**（已在內文改掉）：註解裡寫著 `from 'vitest'` 的是**六支**不是五支（票 `06` 加了 `ui/review-screen.test.tsx`）；`@jest/globals` 被**七支**檔 import 不是四支。另外 `expo/metro-config` 只出現在 `require()` 裡，票原本把它列為 `from` 的子路徑例子，會誤導動工的人只抓 `import`。

**原本四個待決各自的代價**（拍板後留檔，不再影響動工）：

| 待決 | 選了什麼 | 沒選的那邊代價是 |
| --- | --- | --- |
| 一、掃描範圍 | 只掃 `mobile/` | 連 `core/` 整批掃 → 現在就 21 條誤報；追 import 鏈 → 今天收穫為零，且與第四條打架 |
| 二、比對對象 | 查 `package.json` | 查 `node_modules` → 靠別人順便帶進來的套件是綠的，別人升版就消失 |
| 三、`import type` | 算 | 不算 → 範圍窄，但兩次案發沒有一次是執行期爆的 |
| 四、`moduleNameMapper` | 不讀，自管放行名單 | 讀映射表 → 放行 `vitest`，驗收最後一條當場過不了 |

**第一條的第三選項為什麼沒選**（維護者當場問了難度）：從 `mobile/` 追 import 鏈只會追到 12 支左右的 `core/` 檔（`types`／`storage`／`review`／`reading`／`cloud-backup`／`cloud-crypto`／`cloud-crypto-vectors`／`app-error`／`i18n` 那四支／`test-setup`），外部套件只有 `vitest` 一種。也就是說它與「只掃 `mobile/`」在今天結果相同，卻要多寫找檔器、還要去讀 `jest.config.js` 的 `testMatch` 找起點。將來若真的需要，那時再升級成本相同。

### 2026-08-28 — 實作完成，十條驗收全過，狀態轉 `done`

> *This was generated by AI during implementation.*

**新增三樣東西：**

| 檔 | 做什麼 |
| --- | --- |
| `mobile/test/import-scan.ts` | 純邏輯：剝註解、抓 import 來源、換算套件名、比對已宣告清單。不碰檔案系統 |
| `mobile/test/import-scan.test.ts` | 上面那支的單元測試，25 條 |
| `mobile/test/dependency-guard.test.ts` | 真正走檔案樹的守門測試，7 條。放行名單是空的，就寫在這支檔裡 |

外加 `mobile/package.json` 補一行 `@jest/globals: ^29.7.0`（`package-lock.json` 跟著同步）。實際 import 它的是**九支**檔，不是票上寫的七支——票之後我自己又加了兩支測試檔。

**切成兩支檔的理由**：走檔案樹那一半吃的是真實 repo，只驗得到「今天是綠的」；剝註解、子路徑、`require()` 這些規則要能單獨餵假資料才驗得完整。

**單元測試的 fixture 有一條紀律，動它的人要知道**：fixture 裡的套件名一律用 `package.json` 真的有的那些（`react`、`expo-router`……）。因為 `dependency-guard` 掃的是原始碼文字，分不出「這行 import 是真的」還是「這行 import 只是寫在字串裡的假資料」——fixture 寫個不存在的名字，守門那支當場紅。想驗「查不到會被抓出來」時改的是傳進去的 `declared`，不是 fixture 的名字。

**手動做掉的兩條驗收**（改完即還原，`git status` 已確認乾淨）：

- 在 `lib/term-layout.test.ts` 加 `from 'totally-not-installed'` → 紅，訊息是
  `lib/term-layout.test.ts import 了 'totally-not-installed'，但 mobile/package.json 沒有列 totally-not-installed`
- 在 `lib/review-session.test.ts` 加 `from 'vitest'`（重現票 `06`） → 紅，訊息同格式

**已知限制與掃不到的寫法**：見下方 code review 那一則，詳細的寫在 [import-scan.ts] 的檔頭。

**全套跑過**：`mobile/` 是 10 suites／213 tests 全綠、`tsc --noEmit` 乾淨；repo 根是 33 files／626 tests 全綠、`tsc --noEmit` 乾淨。

### 2026-08-28 — code review 兩軸跑完，改了四處，留下三個已知缺口

> *This was generated by AI during code review.*

**照審查意見改掉的：**

| 改了什麼 | 為什麼 |
| --- | --- |
| 加 `SourceFile` 型別 | `{ file, code }` 這對欄位原本在三個地方各寫一次（Data Clumps） |
| `stripComments` 裡的 `quote` 改名 `char` | 走到最後一行時它裝的是任意字元，不是引號，名字在騙人 |
| 三處重抄的數字（54 種來源、20 條 vitest…）改成指回本票 | 抄三份會各自走鐘 |
| 走檔案樹跳過 `.` 開頭目錄的理由寫進註解 | 原本只列了 `.expo`／`.claude` 兩個例子，沒說為什麼整類可以跳 |

**沒照做的一條**：`dependency-guard.test.ts` 讀檔是在模組載入時跑，不在 `it()` 裡。審查建議搬進測試裡好讓失敗指得出是哪一條。沒搬——真的爆掉時 Jest 印的是「Test suite failed to run」加完整堆疊，已經指得出位置，搬進去要多一層記憶化，換不到什麼。

**`package-lock.json` 那行 `fsevents` 的 `"dev": true` 被拿掉，不是這張票造成的。** 實測：拿 `HEAD` 的 `package.json` 與 `package-lock.json`、**一行都不改**，跑 `npm install --package-lock-only` 一樣會拿掉它。而且 npm 算對了——`fsevents` 的上游是 `jest-haste-map`，那支在 lock 裡標的是 `[prod]`（走 `expo` → `metro` 進來），本來就不該掛 `dev`。`HEAD` 那份 lock 在這一格是舊的，這次順手被 npm 更新。手改回去等於跟工具打架，所以留著。

### 三個已知缺口——都不在本票範圍，要不要補是另一張票的事

**一、抓不到「不是 import 語法、但確實會去解析套件」的三種寫法。**

| 寫在哪 | 長什麼樣 |
| --- | --- |
| `lib/app-context.test.tsx` | `jest.mock('expo-file-system', …)` |
| `jest.config.js` | `preset: 'jest-expo/ios'` |
| `babel.config.js` | `presets: ['babel-preset-expo']` |

三個套件今天都列在 `package.json` 裡，所以不紅。但這正是票上那句「本機綠、CI 紅」的同一類寫法——哪天有人在這三個位置寫了沒宣告的套件，這支檢查看不到。

**二、剝註解那支小掃描器不是 JavaScript 剖析器。** JSX 純文字裡的單引號（`<Text>don't</Text>`）與 regex 字面量裡的引號（`/it's/`）都會被它當成字串開頭。`mobile/` 現在兩種都沒有。

**踩到了會怎樣：多報，不會漏報。** 字串內容是原樣抄出去的，所以讀錯邊只會讓那一段裡的註解沒被剝掉，真的 import 一條都不會消失。失手的方向是紅燈太大聲，不是綠燈騙人——那正是這支檢查最不能出的錯，方向是對的那一邊。

**三、`.` 開頭的目錄整類跳過。** 哪天有人把真的原始碼放進點目錄，掃不到。今天實測收到的檔數與 `find` 的全集一致。

### 兩處比票上多做的，留檔說明

- **動態 `import('…')` 也抓。** 票只點名 `from '…'`、`import '…'`、`require('…')` 三種。多這一條是同一個 regex 多一個分支，抓的是同一類東西（會讓 CI 紅的 import），漏掉反而是個洞。
- **多一條「掃出來的外部套件不是零」的測試。** 防的是掃描或抓取那一段哪天壞掉、這支檢查變成「什麼都沒看就說綠」。票上沒要，但這張票的主旨就是不要相信假的綠燈。
