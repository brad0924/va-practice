# 04 — PWA description 那一行的用詞與守門

Status: done
Type: enhancement
Blocked by: 無

不擋任何票。兩件事都只動 `vite.config.ts:48` 那一行，談的時候會互相牽動，所以收在同一張票。

## 這張票怎麼來的

票 `03` 實作完之後跑 `/code-review`（2026-08-19），Standards 軸在同一行上抓到兩件事。**兩件都不是票 03 做錯**——第一件是票 03 逐字指定的字串，第二件從票 02 那輪就存在。兩件都超出票 03 的範圍，因此另開一張。

那一行現在長這樣：

```ts
description: 'Japanese vocabulary flashcards. Build your own books, review at spaced intervals, works offline',
```

它是 PWA（Progressive Web App，漸進式網頁應用）manifest 的說明欄，build 出來是 `dist/manifest.webmanifest` 的 `"description"`。

**看得到它的地方只有一處**：瀏覽器問「要不要把這個網站安裝成 app」時，那個對話框把它印在名字底下。安裝完就不再出現。

**iOS 版完全用不到它。** `vite.config.ts` 的 VitePWA 掛在 `isIOS ? [] : [...]` 裡，`build:ios` 不產生 manifest。

**但它是這個專案唯一一句對外的說明文字。** `index.html` 沒有 `<meta name="description">`，只有 `<title>` 與 `apple-mobile-web-app-*` 那幾個。

## 第一件：`books` 不是 glossary 釘的正名

`docs/glossary.md` 第一列釘的英文正名是 `Vocabulary Book`，避用欄是 ~~Deck~~、~~Folder~~。

那一行寫的是 `Build your own books`。**它沒有踩到避用欄**，但也不是正名。實際的影響是使用者在安裝提示看到 `books`，打開 app 之後英文介面說的是 `Vocabulary Book`——同一個東西在門口跟屋裡兩個叫法。

`docs/glossary.md` 檔頭寫明：條目清單有 `src/lib/glossary.test.ts` 釘住，**但譯名寫得對不對測不出來，那要人看**。所以這種漂移只能靠人抓，這次就是 review 抓到的。

**要決定的是改不改。** 改成 `Build your own vocabulary books` 會讓那一行變長；不改則接受門口與屋裡兩個叫法。

**這條字串是票 `03` 的 `## 決定` 逐字寫死的**，改它等於推翻票 03 談定的一條，不是實作時的自由選擇。這也是為什麼它沒有在票 03 那輪順手改掉。

## 第二件：這一行也寫著名字，但沒人守它

`ADR-0012` 把顯示名稱抵達的位置分成兩套：打包流程內的用 `import`，流程外的靠 `src/lib/app-name.test.ts` 守門。

`vite.config.ts` 同一個檔案裡，上方的 `name` 與 `short_name` 都吃 `APP_NAME` 常數。**只有 description 這一行是手打的字面值**，而它開頭的 `Japanese vocabulary` 跟 `APP_NAME.full`（`Japanese Vocabulary Cards`）內容重疊。

**注意編號：`ADR-0012` 說的「七處」指的是七個檔案，`vite.config.ts` 已經算在裡面。** description 是那個檔裡第二處寫著名字的地方，不是第七個位置。

下次改名時，`name` 與 `short_name` 自動跟上，這一行會靜默停在舊字——**而且沒有任何東西會抓到**。這正是 `ADR-0012` 否決「寫 script 自動改寫」時描述的那種破口，只是位置不同。

**要決定的第一個問題是：這算不算票 `03` 說的「不改單一來源的機制」。** 票 03 的「這張票不做的事」寫著：

> **不改單一來源的機制。** 若實作時覺得「`Info.plist` 跟兩份 `privacy.html` 還要手動改很煩，寫支 script 自動化」——先讀 `docs/adr/0012-display-name-single-source.md`，那條路已經評估過並否決

那一條擋的是「寫 script 自動化」。**加一條守門測試是不是同一件事，這張票要先答**。`ADR-0012` 自己的立場是「全面自動化若要安全，最後還是得長出守門測試；那不如直接只做守門測試」——照這句讀，加守門是順著 ADR 走，不是推翻它。但那句講的是 `Info.plist` 與 `privacy.html`，沒講到這一行。

**答完之後才輪到怎麼做。** 幾個方向都還沒評估過，也還沒排除彼此：

- 讓 description 也吃常數，用模板串起來（例如 `` `${APP_NAME.full} flashcards. …` ``）
- 維持字面值，在 `app-name.test.ts` 加一條「description 要包含 `APP_NAME.full`」
- 什麼都不做，理由是這一行只在安裝提示出現一次，過期的代價很小

第三條要算清楚代價再收下，不是預設答案。

## 決定

以下三條是 2026-08-19 那輪 triage 談定的，不是實作時的自由選擇。

### description 改成模板字串，開頭吃 `APP_NAME.full`

```ts
description: `${APP_NAME.full}. Build your own vocabulary books, review at spaced intervals, works offline`,
```

引號從 `'` 換成反引號，才插得進變數。

**這推翻票 `03` 的「PWA description」一條。** 票 03 定的是 `'Japanese vocabulary flashcards. Build your own books, …'`，兩處都變了：

- 開頭的 `Japanese vocabulary flashcards` 換成常數，`flashcards` 這個字消失。**接受這個代價**——名字本身的 `Cards` 已經承擔「這是卡片 app」的訊息，而換來的是這一行永遠不可能與名字不一致。
- `books` 補成 `vocabulary books`，跟上 `docs/glossary.md` 第一列的正名 `Vocabulary Book`。**票 03 當初縮短它的效果是安裝提示與 app 內英文介面兩個叫法**，這輪把它收掉。原本擔心的「`vocabulary` 在一句裡出現兩次」在新句子裡不成立，開頭已經不再是 `Japanese vocabulary`。

### 這不算票 `03` 擋的「改單一來源的機制」

`ADR-0012` 的判準只有一句話：**這個檔有沒有辦法吃到 TypeScript 常數**。吃得到的直接 `import`，吃不到的才手打加守門。

`vite.config.ts` 就在「吃得到」那一組，而且它早就 `import` 了 `APP_NAME`，`name` 與 `short_name` 兩欄也已經在用。**只有 description 這一行漏接。**

所以這輪做的不是發明新規則，是把漏接的一行補回既有機制。規則本身一個字都沒改，**`ADR-0012` 因此不必更新**。

### 不加守門測試

被否決的是「維持字面值，在 `app-name.test.ts` 加一條測試釘住 description」。

**`ADR-0012` 的 Considered Options 已經否決過這條路**——第一條「全部寫字面值，單靠守門測試」的否決理由是：測試得去解析 `vite.config.ts` 的原始碼抓字串，換行、改引號就可能認不出來，而那個檔明明 `import` 就徹底解決了。同樣的理由在這一行上完全成立。

走這條路還要連帶在 `ADR-0012` 補一段說明為什麼這處是例外。上面那條決定不需要動 ADR，這條需要，這也是差別之一。

## 這張票不做的事

- **不改 `APP_NAME` 的兩個值。** 票 `03` 剛談定並實作，商店三格也已存檔
- **不改 `lang: 'en'`。** 那是 `.scratch/i18n/spec.md` 決定九的產出，與本票無關
- **不碰 `Info.plist` 與兩份 `privacy.html` 的守門方式。** `ADR-0012` 那套照設計運作，票 03 那輪紅 12 項、指出 9 個位置，一處都沒漏
- **不補 `index.html` 的 `<meta name="description">`。** 上面只是把「這是唯一一句對外說明」講清楚，要不要補是另一件事，要開就另開票
- **不改 `docs/adr/0012-display-name-single-source.md`。** 見「決定」第二條：規則沒變，只是補上漏接的一行
- **不改票 `03` 的檔案。** 它是決策紀錄，本來就該留著當時談定的字。本票推翻它哪一條，寫在本票的「決定」裡
- **不動 `docs/glossary.md`。** 這輪是讓 description 跟上那張表，不是改那張表

## 待決

- [x] `books` 改不改成 `vocabulary books`（推翻票 `03` 的一條決定）→ 改，見「決定」第一條
- [x] 加守門算不算票 `03` 擋的「改單一來源的機制」→ 不算，見「決定」第二條
- [x] 若要守，用模板吃常數還是加測試釘住 → 模板吃常數，見「決定」第一、三條

## Comments

### 2026-08-19：triage 談定，轉 ready-for-agent

> *This was generated by AI during triage.*

三個待決都收斂了，`## 決定` 一節是結論的正本。

triage 期間查證了三件事：

1. **沒有重複實作。** `grep` 過 `src/` 與 `scripts/`，沒有任何測試讀 `vite.config.ts`，也沒有任何測試碰 manifest。這個破口是真的。
2. **`ADR-0012` 的判準已經回答了第二個待決**，不必推論——`vite.config.ts` 被明確歸在「打包流程內 → 直接 `import`」那一組。
3. **第三個方向（加測試）`ADR-0012` 已經否決過**，理由是解析原始碼的測試脆弱。它不是新選項，是重開已經關掉的門。

另外修掉了本票原本「這一行是第七處名字」的說法——`ADR-0012` 的七處指的是七個檔案，`vite.config.ts` 已在其中。

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** PWA manifest 的 description 改成模板字串吃 `APP_NAME.full`，並把 `books` 補成 glossary 的正名 `vocabulary books`

**Current behavior:**
PWA manifest 設定裡的 `description` 是一條手打的字面值字串。同一份 manifest 設定的 `name` 與 `short_name` 都取自 `APP_NAME` 常數，只有 `description` 自己重寫了一次名字。改名時前兩欄自動跟上，`description` 會靜默停在舊字，沒有任何測試或檢查會發現。

字串裡另外用了 `books` 指稱「單字本」，而 `docs/glossary.md` 釘的英文正名是 `Vocabulary Book`。使用者在瀏覽器的安裝提示看到 `books`，進到 app 的英文介面看到 `Vocabulary Book`。

**Desired behavior:**
`description` 改用模板字串，開頭直接取 `APP_NAME.full`，其餘句子維持原有的三段結構（自建單字本／到期複習／離線可用），並把 `books` 改成 `vocabulary books`。

最終字串等於：

```
Japanese Vocabulary Cards. Build your own vocabulary books, review at spaced intervals, works offline
```

但**必須由常數組出來，不可以把上面這串直接手打進去**——這條的整個重點就是那個名字不再有第二份副本。

**Key interfaces:**
- `APP_NAME`（`src/lib/app-name.ts` 匯出的常數）— 不改它的值，只是多一處使用者。PWA manifest 設定的檔案本來就已經 `import` 它
- PWA manifest 的 `description` 欄 — 型別不變，仍是字串，只是改由模板組出

**Acceptance criteria:**
- [x] manifest 設定裡的 `description` 是模板字串，`APP_NAME.full` 以插值形式出現，字串裡沒有手打的 `Japanese Vocabulary Cards`
- [x] `npm run build` 產出的 `dist/manifest.webmanifest`，其 `"description"` 完全等於 `Japanese Vocabulary Cards. Build your own vocabulary books, review at spaced intervals, works offline`
- [x] 同一份 manifest 的 `"name"` 與 `"short_name"` 沒有變動
- [x] `npm run typecheck` 乾淨
- [x] `npm run test` 全綠（本票不新增測試，既有的應該一項都不受影響）
- [x] `npm run build:ios` 仍然不產生 manifest，且無 `{{APP_NAME_*}}` 佔位符殘留

**Out of scope:**
- 不新增任何守門測試。`src/lib/app-name.test.ts` 一個字都不要動——理由見本票「決定」第三條，那條路 `ADR-0012` 已經否決
- 不改 `docs/adr/0012-display-name-single-source.md`
- 不改 `APP_NAME` 的 `short` 或 `full`
- 不改 manifest 的 `lang`、`start_url`、`scope`、顏色或圖示
- 不補 `index.html` 的 `<meta name="description">`
- 不改 `docs/glossary.md`
- 不改票 `03` 的檔案

### 2026-08-19：實作完成，六條驗收全過

`vite.config.ts` 的 description 改成模板字串，開頭插 `${APP_NAME.full}`，引號換成反引號。**整個 repo 只動這一行**（`git diff --stat`：`vite.config.ts | 2 +-`），沒有新增測試、沒有動 `ADR-0012`、沒有動 `app-name.test.ts`。

驗證結果：

- `npm run typecheck` 乾淨
- `npm run test` 551 項全綠（29 個測試檔，與票 `03` 那輪同數，一項都沒受影響）
- `npm run build` 的 `dist/manifest.webmanifest`：`"description"` 為 `Japanese Vocabulary Cards. Build your own vocabulary books, review at spaced intervals, works offline`，`"name"` 與 `"short_name"` 未變動
- `npm run build:ios` 不產生 `.webmanifest`，`dist/index.html` 零佔位符殘留，分頁標題 `Japanese Vocabulary Cards`、`apple-mobile-web-app-title` 為 `JP Vocab`

**至此 `Japanese Vocabulary Cards` 在 repo 內只剩 `src/lib/app-name.ts` 一份字面值**（`.scratch/` 的決策紀錄不算）。改名時 manifest 三欄全部自動跟上。
