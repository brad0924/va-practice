# 01 — React Native 複習畫面探路原型

Status: wontfix
Type: enhancement
Blocked by: 無，可立即開始

> ## 判定：分不出來，停（2026-08-18）
>
> 原型做出來了、編成獨立 app 裝上真機了、盲測執行了。**維護者分不出哪個是原生的。**
>
> 依本票預先寫死的停損條件：**這個 app 維持 Capacitor 包殼，不改寫成 React Native。**
> 結論要收錄進 `docs/adr/0015-capacitor-ios-shell.md` 的 Consequences，提醒已補在
> `../../ios-app/issues/10-adr-and-context-vocabulary.md`。
>
> 完整判定、盲測怎麼做的、以及過程中挖到的技術結論，見底下 `## Comments`。
> 這張票**不是白做**——它買到的東西寫在那裡。

> ## 這張票在 Windows 上做得完
>
> 這是它跟前一版（`.scratch/swiftui-spike/issues/01`，已 `wontfix`）最大的差別。SwiftUI 那條要 macOS、Xcode、一台 Mac 加一個週末；React Native 用 Expo 開發、EAS Build 在雲端編譯，維護者現在這台 Windows 就做得完。
>
> 需要：Node.js（本機由 nvm 管理，版本用 `nvm list` 確認）、Expo 帳號（EAS Build 免費額度）、已設定的 Apple 開發者帳號（票 `ios-app 17` 那批簽章憑證即是）、一支能裝開發版的 iPhone。

本功能沒有 spec.md——決策背景在 `.scratch/swiftui-spike/issues/01`（含它的 `## Comments`）與 `../../ios-app/spec.md` 決定一底下的訂正。

## 為什麼有這張票

維護者想把這個 app 改成原生。前一版票把動機釘成「想寫原生」就收工了，**這次拆得更細，結論不同**：

- **想學新語言** → React Native 買不到（還是 TypeScript）
- **畫面要是系統原生元件** → React Native **買得到**（`<Text>` 變成 `UILabel`、`<ScrollView>` 變成 `UIScrollView`，捲動手感、選字放大鏡、動態字級、VoiceOver 全由系統提供）

維護者要的是第二個。所以 SwiftUI 那條退場，Flutter 一併出局——Flutter 用自繪引擎，跟 WebView 同樣不使用原生元件，犯的是同一條。

### 判準沒有變軟

前一版票挖出來的那件事**今天依然成立**：問「哪裡用起來不順」，答案是**講不出來**。而且現在證據更強——`ios-app` 的 16 張票已 `done`（Keychain、原生日語語音、觸覺、每日提醒推播），維護者每天都在用那一版，仍然講不出不順。

所以這張票的判準跟前一版一樣：**不是「改完會不會變好」，是「這是不是好的時間花法」。** 停損條件照樣現在就寫死。

### 為什麼不直接重寫

現有的 app 有 **6,011 行程式碼、6,051 行測試、452 個測試**，網頁版在 GitHub Pages 上跑著，上架流程只差 `ios-app 10` 與 `20` 兩張票。

React Native **不是**從零重寫——純邏輯 3,638 行與其 4,732 行測試同為 TypeScript，可以搬。要重寫的是畫面層 2,373 行，作廢的是畫面測試 1,319 行。

但有三處接不上，而且**都還沒有人驗證過**：

1. **振假名沒有對應物。** `src/ui/reading-html.ts`（29 行）用瀏覽器內建的 `<ruby>`／`<rt>` 六行解決。RN 的 `<Text>` 不暴露 iOS 的 `CTRubyAnnotation`。
2. **`localStorage` 同步，`AsyncStorage` 非同步。** 純邏輯層有 27 處，而它是唯一真相來源（ADR-0002），改動會傳染到每一個呼叫端。
3. **Web Crypto 沒有對應物。** `src/lib/cloud-crypto.ts`（102 行）要換套件重刻，且必須與網頁版**位元級相容**——網頁版要留著，這是硬約束。

**第 1 項是這張票要回答的。** 第 2、3 項留給重寫真的開始之後，這次不碰。

## 要做什麼

用 React Native 做出**複習畫面**，對照 `src/ui/review-view.ts`（175 行）：

| 部位 | 內容 |
| --- | --- |
| 標題列 | 「剩餘 N 張」＋單字本開關（開關可以是死的，不用真的能切） |
| 卡片 | 詞條（**含振假名**，漢字上方標假名）；掀開後顯示釋義 |
| 底部 | 未掀開時一顆「顯示答案」；掀開後換成四顆評分鈕：**再次／困難／好／簡單** |
| 完成狀態 | 三張都答完後顯示「今日份完成」 |

**寫死三張假卡**，其中至少一張要有漢字（才看得到振假名）、一張純假名。按評分鈕就換下一張，不做任何排程計算。

外觀要**盡量對齊現有的網頁版**——這不是美化，是盲測的前提：兩版長得越接近，盲測量到的才越接近「平台差異」而不是「設計差異」。配色抄 `src/styles.css`（`--bg: #141821`）。

### 振假名是這張票的技術核心

RN 沒有 ruby。兩條路，**先試第一條**：

- **兩層 `<Text>` 疊字**：假名一行、漢字一行，用 `lineHeight` 與對齊把假名壓到漢字正上方。純 JavaScript，不寫原生碼。風險是換行、字距、不同字級下會不會跑掉。
- **自寫原生模組包 `CTRubyAnnotation`**：Core Text 原生支援振假名，效果一定對。代價是要寫 Swift（未來還要再寫一份 Kotlin），而且 EAS Build 得改用 development build 流程。

如果第一條做出來在真機上看起來歪掉、或字級一調就爆，**那本身就是一個結論**，寫進 `## Comments`，不要硬撐到第二條——那已經超出探路的範圍。

### 由 agent 寫，不是維護者寫

維護者是 React Native 新手。自己寫的第一版很可能比現有的網頁版還醜——那樣盲測量到的會是熟練度，不是平台差異，判準就被污染了。

維護者在這張票裡**只負責盲測與判定**，不寫程式。

## 這張票不做的事

- **不接排程、不接儲存、不接雲端。** 沒有 SM-2、沒有到期日、沒有 localStorage 對應物、沒有 Firebase。三張卡寫死在程式碼裡
- **不碰 `localStorage` 轉非同步、不碰加密相容。** 那兩項是重寫真的開始之後的事
- **不碰現有的任何程式碼。** `src/`、`ios/`、`vite.config.ts` 一行都不動
- **不承諾重寫。** 這張票結束時唯一的產出是一個決定，不是一個 app
- **不做 Android。** 維護者說 iOS 上架後才要，而且還沒決定 Android 是否也必須是原生元件

## 原型放哪

分支 `prototype/rn-review-screen`，從 `main` 開出去。沿用這個 repo 既有的 `prototype/*`／`throwaway/*` 慣例（見 `prototype/stats-snapshot`、`throwaway/toast-styling-prototype`）。做完砍掉，不進 `main`。

Expo 專案跟純前端 repo 混在一起確實怪，但那是暫時分支，可接受。

## 盲測怎麼做

**這是這張票的核心，不是附註。**

維護者的動機是「我知道它是 WebView，就覺得不對」——**這句話等於承認偏誤確定存在**。如果拿著兩台、心裡清楚哪台是原型，一定會覺得它比較好，那判準就沒有否決能力。所以必須盲測：

1. **原型要編成獨立的 app**，用 **EAS Build**，不能用 Expo Go——Expo Go 有自己的圖示與啟動畫面，一眼就露餡，盲測直接破功
2. **兩版都裝上同一支 iPhone**：RN 原型從 EAS Build 裝；Capacitor 版用已安裝的那一份
3. **網頁版先對齊**：在現有 app 裡手動建一本、加上原型那三張一樣的卡，讓它進到複習畫面時內容相同（不用改任何程式碼）
4. **不標示哪個是哪個**，兩個圖示放在一起、名字都遮掉或排在一起分不出來的位置
5. **隔一天再拿起來滑**，不要當天做——剛做完的印象太強
6. 判定：**分不分得出來哪個是原生的**

## 判定之後

**分得出來** → 這個方向值得投入，才進入下一串問題（`localStorage` 轉非同步怎麼做、加密相容怎麼驗、6,051 行測試怎麼辦、重寫要多久）。**那幾題現在刻意不釘**，沒通過盲測就不需要答案。

**分不出來** → **停。** 這個 app 維持 Capacitor 包殼，把結論寫進 `ios-app 10` 要開的那份 ADR（`docs/adr/0015-capacitor-ios-shell.md`）的 Consequences。想寫原生的欲望另外找小專案滿足，不由這個 app 承擔。

**停損條件現在就寫死，是因為做完當天的維護者已經花了時間，那時候的他不會想承認白做。**

## 這張票與上架的關係

**互不阻擋。** Capacitor 版繼續走完 `ios-app 10`、`20`、`11` 送審上架，這張票同時進行。維護者已明確決定先上架——App Store 上先有東西，之後不論結論如何都不吃虧。

## 驗收

- [x] `prototype/rn-review-screen` 分支上有可跑的 Expo 專案
- [x] 原型能用 EAS Build 編成**獨立 app**（不是 Expo Go）並裝上真機 — `eas.json` 的 `blind-test` profile，內部發佈、沿用既有的發佈憑證
- [x] 原型跑得起來：三張卡、掀開答案、四顆評分鈕、換下一張、完成狀態
- [x] **振假名正確標在漢字正上方** — 真機上假名字身底端與漢字字身頂端的距離兩版皆為 15 device px，一模一樣；換行已驗
- [x] 若振假名走了原生模組那條路，`## Comments` 要記下為什麼兩層 `<Text>` 不夠 — 沒走原生模組，但兩層 `<Text>` 的代價記在底下
- [x] 外觀已對齊網頁版到「值得盲測」的程度 — 用真機截圖逐項量過，見底下
- [x] Capacitor 版已建好對應的三張卡
- [x] 盲測已執行：兩版都在真機上、未標示 — **但判定在當天，不是隔天**，見底下「這次盲測有一處沒照票走」
- [x] 判定結果與理由寫進本檔 `## Comments`
- [x] 若判定為「分不出來」，本票轉 `wontfix` 並註明結論，且已在 `ios-app 10` 補一行提醒 ADR 要收錄這個結論

## Comments

### 2026-08-18 — 原型寫好了，外加一個打在本票立論上的發現

原型在分支 `prototype/rn-review-screen` 的 `rn-prototype/`，兩個 commit。**做完就砍掉，不進 `main`。**
怎麼跑、怎麼編、盲測前要先做什麼，全寫在 `rn-prototype/README.md`。

#### 振假名：兩層 `<Text>` 這條路走得通，位移量是量出來的

沒有走原生模組那條。做法是把詞條切成一欄一欄，每欄上假名下本文，整排 `flexWrap` 換行；
未標音的部分逐字一欄，換行才只發生在字與字之間，不會把某一欄的讀音跟本文拆散。

三個位移量不是調到順眼為止。在 Chrome 上把網頁版 `.term` 與 `.term rt` 的實際框量出來：
**假名的字身底端與漢字的字身頂端落在同一條線上**（相差 0.02px），也就是瀏覽器把假名貼著
漢字頭頂放、中間不留縫。照這個數字回推，原型量到的縫是 **0.00px**。另外補回漢字底下
0.45em 的行距與換行時的行距，疏密才與網頁版一致。

#### **這條路要付的代價：長按選字沒了**

**這不是實作沒做好，是這條路本身的代價，而且它打在本票的立論上。**

本票第 20 行把「畫面要是系統原生元件」列為唯一買得到的動機，並點名了**選字放大鏡**與
VoiceOver。但振假名靠疊字做的話，詞條就一定被拆成好幾個 `<Text>`，而 iOS 的長按選取
只在單一個 `<Text>` 內成立——**整串詞條選起來複製去查字典，做不到了**。網頁版
`src/styles.css:78` 特地寫明「`.term` 與 `.meaning` 完全不碰」，保的正是這個功能。

VoiceOver 補得回來（整排掛 `accessibilityLabel`，會把詞條當一個東西唸完）。選取補不回來。
兩者都要保住，只剩本票列的第二條路——自寫原生模組包 `CTRubyAnnotation`。

**盲測時請刻意長按詞條試一次。** 這是目前已知最明顯的破綻，而且它剛好落在本票聲稱
買得到的那幾項裡面。

#### 外觀：量過，不是看起來像

在瀏覽器上把兩邊的實際尺寸逐項比對：標題列 48px、卡片 65–673、按鈕 690–753、
`--tap` 52、單字本開關 32 高／13.6px 字／圓角 999、卡片圓角 16 內距 24/16——都一致。
配色與圓角照抄 `src/styles.css` 的 `:root`。

#### 三件票上沒寫、但不做會毀掉盲測的事

1. **介面語言接了三語。** 網頁版的語言跟著裝置走（ADR-0013）。原型若寫死繁中，
   你的 iPhone 一旦不是繁中，兩台擺在一起一眼分得出來。只收複習畫面用得到的 11 條。
   **注意**：原型讀不到 `va-practice:lang`（那在網頁版自己的 `localStorage` 裡），
   所以它等同網頁版選在「系統預設」時的行為——**盲測前請確認 Capacitor 版沒有手動選過語言**。
2. **朗讀做成真的會念。** 死鈕一按就露餡。挑品質最好的日文語音、語速預設的 0.9 倍，
   與 `ios/App/App/SpeechPlugin.swift` 對齊。
3. **圖示、啟動畫面、app 名稱、狀態列全部對齊 Capacitor 版。** 圖示與啟動圖是位元級複本；
   名稱同樣是 `VocabCard`；狀態列跟著系統走，因為 Capacitor 版沒設 `UIUserInterfaceStyle`。

單字本開關與「編輯」是死的（票上同意開關可死；「編輯」留著是因為網頁版有，少一顆就是破綻）。

#### 還沒有人驗過的一件事

`readingPull` 那個位移假設 `lineHeight` 多出來的空間是上下平分的。這在瀏覽器上量到確實如此，
**在 iOS 上沒有人驗過**——`UIKit` 有可能把多出來的空間全部放在字的上方。假名離漢字太遠
或壓到漢字，調的就是這個值，位置在 `rn-prototype/src/Term.tsx` 的 `termMetrics()`。

字級變化同理：倍率已經接上 `useWindowDimensions().fontScale`（而不是只讀一次的
`PixelRatio.getFontScale()`），但只在瀏覽器上驗得到，真機要自己調一次系統字級看看。

#### 沒動到既有的任何東西

`src/`、`ios/`、`vite.config.ts` 一行未改。主專案的 `npm run typecheck` 與 527 個測試全綠。

### 2026-08-18（同日稍晚）— 判定：**分不出來，停**

原型編成獨立 app 裝上真機，與 Capacitor 版並存。**維護者蒙著開其中一個，滑完整張卡，
在做出判斷之前分不出來哪個是原生的。**

依本票開頭就寫死的停損條件：**維持 Capacitor 包殼，不改寫成 React Native。**

#### 最值得留下來的一句

盲測之前，維護者把兩版並排、且知道哪台是哪台，**指得出三處差異**：字比較小、位置比較下面、
圖示不一樣。那三處後來查出來都是真的（字體沒指定、分隔線寬度是實作 bug、圖示抄錯來源）。

盲測時那三處**全部修好了**，但重點不在這裡——重點是：

> **同一組差異，標示著就看得見，蒙起來就消失。**

本票第 92 行引的那句「我知道它是 WebView，就覺得不對」，等於承認偏誤存在。這次盲測把那個
偏誤量出來了：**它買到的是知識，不是感知。**

#### 這次盲測有一處沒照票走

**判定在當天，不是隔天。** 票上第 98 行寫「隔一天再拿起來滑，不要當天做——剛做完的印象太強」。

這個偏差**不利於「分不出來」這個結論**，因此不構成翻案理由：印象最強、最熟悉的當下最容易
分辨，那時候都分不出來，隔一天只會更分不出來。若偏差的方向相反（隔了一週才測、然後說分不出來），
才需要重測。

測的是修完四項差異之後的版本（分隔線寬度、字體、圖示來源、標題列文字）。

#### 揭曉是怎麼做的

沒有找第二個人。兩個 app 圖示、名稱、啟動畫面完全相同，都收進 App 資料庫、主畫面上不留，
用 Spotlight 搜尋開啟（會出現兩列同名同圖示，順序由 iOS 決定），擲硬幣決定點哪一列。

**揭曉靠長按釋義**：網頁版可以選取（`src/styles.css:78` 特地保留），RN 原型完全沒反應。
規則是「判斷寫下來之前不准長按」——因為那個差異是兩層 `<Text>` 疊字造成的實作限制，
不是「原生 vs WebView」造成的，拿它當判斷依據會答錯題。

這個自助辦法只夠做一輪：第二輪起你已經知道其中一個長什麼樣了。票要的本來就是一個是／否的
判斷而不是正確率，所以夠用。

#### 這張票買到了什麼（不是白做）

1. **一個原本無法否證的直覺，變成了一個有答案的問題。** 「改成原生會不會比較好」問不出結果；
   「蒙起來分不分得出來」問得出來。
2. **振假名那題有答案了。** 兩層 `<Text>` 疊字在 iOS 上做得出來，位置與網頁版的 `<ruby>` 一致
   （假名字身底端貼著漢字字身頂端，兩版量到的距離都是 15 device px）。日後若有人再提原生重寫，
   這題不必重問。
3. **但它有代價，而且打在本票的立論上**：詞條被拆成多個 `<Text>` 之後，跨字長按選取沒了。
   本票第 20 行把「選字放大鏡」列為 RN 買得到的東西之一——**實際上這條路會弄丟它**。
   兩者兼得只剩自寫原生模組包 `CTRubyAnnotation` 那條路。
4. **`ios-app 10` 那張票的 ADR 有東西可寫了。** 它原本卡在「RN 這條路尚未定案」。

#### 後續

- [x] `ios-app 10` 已補上提醒與驗收項，ADR-0015 的 Consequences 要收錄這個結論與它的證據
- [ ] `prototype/rn-review-screen` 分支待砍（維護者決定時機）
- 想寫原生的欲望另外找小專案滿足，不由這個 app 承擔——本票第 105 行
