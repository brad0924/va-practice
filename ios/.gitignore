App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files
App/App/capacitor.config.json
App/App/config.xml

# `cap sync` 每次都會砍掉重建的符號連結，指向的是這台機器上的絕對路徑。
# 進版控只會害下一台機器拿到一堆斷掉的連結（見 .scratch/fixed-gemini-key/issues/01）。
App/CapApp-SPM/symlinks
