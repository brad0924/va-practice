# JLPT 單字複習工具

一個純前端的日文單字閃卡工具。使用者把自己的單字收進一本本單字本，複習時看到日文詞條、自評熟悉度，系統據此安排下次複習的日子。

## Language

### 單字本與卡片

**單字本（Vocabulary Book）**：
卡片的容器。一張卡剛好屬於一本，不能同時屬於多本，也不能無家可歸。名字由使用者自取、可隨時改；刪掉一本，裡面的卡與進度一併消失。
_Avoid_: 牌組（本專案裡專指那副隨程式發佈的初始卡片，已退場）、分類、標籤、資料夾

**詞條全域唯一**：
同一個詞條在整個 app 裡只會有一張卡，跨單字本亦然。想讓另一本擁有某個詞，只能把那張卡搬過去。比對基準是去掉讀音標記後的詞條原文，釋義不參與。
_Avoid_: 去重、防呆、唯一鍵

**複習範圍／列表範圍／統計範圍**：
三個畫面各自獨立的單字本選擇。三組都是複選、都至少要有一本、都隨資料備份到雲端。改其中一組不影響另外兩組。
_Avoid_: 篩選器（會讓人以為只是暫時的視圖）、模式、書籤

**卡片（Card）**：
一個日文詞條與其中文釋義的配對，是複習的最小單位。詞條、釋義、以及詞條裡每個漢字的讀音都必填——自己新增或編輯時缺一張卡就存不下去。匯進來的卡不受此限，因此資料裡仍可能有沒讀音的卡，那些卡下次被打開編輯時才會被要求補齊。
_Avoid_: 單字、題目、詞彙

**詞條**：
卡片正面顯示的日文原文，可能是單字、也可能是文法句型（如「ざるを得ない」）。
_Avoid_: 單字（並非全部都是單字）

**釋義**：
卡片背面顯示的繁體中文說明。
_Avoid_: 翻譯、定義、意思

**讀音**：
詞條中各個漢字的假名讀法，以振假名形式標在漢字上方。詞條裡只要有漢字就一定要有讀音，一個漢字都不能漏；不知道讀音就存不了那張卡。純假名與外來語詞條沒有漢字，因此也沒有讀音，不受此限。
_Avoid_: 注音、發音、拼音

**讀音標記**：
在詞條原文中標示讀音的寫法，漢字後接方括號，如 `焦[こ]がす`。所有卡共用這套寫法，不分它在哪一本、是手打的還是匯進來的。
_Avoid_: furigana 語法、ruby 語法、標注格式

**讀音格**：
編輯畫面上對應一段連續漢字的輸入格，一格填一段漢字的假名。多字的漢字串預設逐字開格，使用者可把相鄰的格合併、或把合併的格切開，藉此決定假名分配到哪些漢字上；儲存時各格組回讀音標記。每一格都要填，留一格空就存不下去。純假名的詞條不長出任何格。
_Avoid_: 欄位、輸入框（太泛）、切割（聽起來像破壞資料）、furigana 編輯器

**必填格**：
編輯畫面上非填不可的那幾格：詞條、每一個讀音格、釋義。三種格共用同一句「還空著」的定義（去除頭尾空白後沒有字），換欄鍵要把游標送去哪、按下儲存時先擋哪一格，都由它回答。不碰畫面，因此可以獨立測試。
_Avoid_: 驗證器、必填欄位（太泛）、輪詢器（把手段寫進名字）

**讀音預填**：
詞條輸入完畢後，程式代為填入讀音格的動作，連同各串漢字要開幾格一起代為決定。填進來的仍是使用者的草稿——可改、可清空，儲存前一律要人工確認；程式不承擔「讀音一定正確」的責任。需要使用者另行設定才會啟用，沒設定時完全不發生，讀音格一律留空給人填。
_Avoid_: 自動產生、翻譯、辨識（都會讓人以為程式對結果負責）、AI（Artificial Intelligence，人工智慧）標音（把手段寫進名字）

**讀音編輯器**：
編輯畫面背後那台管著讀音格的機器。持有詞條與各格的內容，決定什麼時候該重建格子、什麼時候該去讀音預填、預填回來的東西收不收。不碰畫面，因此可以獨立測試。哪一格還空著不歸它管，那是必填格的事——它的儲存只做假名檢查與組出讀音標記。
_Avoid_: 狀態機（太泛）、控制器、讀音草稿（會與「詞條加上一組讀音格」這份資料結構混淆）

### 排程

**到期（Due）**：
一張卡下次該出現的日期，只到日、不含時刻。只有已到期的卡會進入當日複習佇列，未到期的卡完全不出現。「再次」評分是例外：到期日設為當天而非「複習日期 + 間隔」，讓這張卡在同一天內即使重整頁面也仍會留在佇列裡；間隔本身不受影響，仍是下次正式評分（好／困難／簡單）的長期排程基準。
_Avoid_: 頻率、權重、機率、時間點

**逾期（Overdue）**：
到期日已經過去、但還沒被複習的卡。逾期卡在排程上與今日到期的卡毫無差別，一樣進入當日複習佇列；兩者只在卡片列表上分屬不同的時間桶，逾期卡歸「現在」、今日到期歸「<24小時」。列上顯示的是該卡實際的到期日，逾期卡因此顯示一個已經過去的日期。
_Avoid_: 過期、遲到、積欠、漏掉

**間隔（Interval）**：
一張卡從這次複習到下次到期之間的天數。每次作答後被放大或縮小。
_Avoid_: 週期、天數

**成長倍數（Ease）**：
每張卡專屬的倍率，決定間隔每輪成長多快。初始 2.5，答得吃力會被調降，下限 1.3。
_Avoid_: 難度、係數、ease factor、EF

**評分（Rating）**：
使用者看完答案後對自己的自評，共四級：再次、困難、好、簡單。
_Avoid_: 分數、quality、q 值、難度選擇

**複習佇列**：
當日所有已到期卡片組成的待複習清單。清空後即為當日完成。
_Avoid_: 待辦、排程、queue

**新卡**：
從未被複習過的卡，尚無間隔與到期日。
_Avoid_: 未學、生字

**複習卡**：
至少作答過一次、已有間隔與到期日的卡。
_Avoid_: 舊卡、已學

**抖動（Fuzz）**：
算出間隔後施加的隨機偏移，避免大量卡片的到期日聚集在同一天。
_Avoid_: 亂數、偏移、隨機化

**到期排序**：
卡片列表的排列方式，可由使用者在兩個方向間切換。正序是最急的排最前，倒序整個反過來。新卡沒有到期日，一律視為最早到期，因此正序時排在所有卡之上、倒序時沉到最底。同一天到期的卡、以及新卡彼此之間，在兩個方向下都維持同一個相對順序。方向同時決定時間桶的順序：倒序時標頭的順序與各桶內的卡片順序一起顛倒。只改變列表的呈現，複習佇列的出題順序不受影響。
_Avoid_: 優先度、排程順序、佇列順序（都會讓人誤以為會影響複習時誰先出現）

**時間桶**：
卡片列表把卡片依到期的遠近分成的六組：新、現在、`<24小時`、明天、`<1週`、未來。每組一個可點的標頭，上面顯示該組張數，卡片收在標頭底下，進入卡片列表時全部收合。搜尋時全部展開、沒有結果的組暫時隱藏。只是卡片列表的呈現方式，不影響排程與複習佇列。
_Avoid_: 分類、狀態、標籤（升格成標頭之前的舊叫法）、分頁

### 資料

**備份（Backup）**：
把整份單字本、卡片、進度與三組範圍選擇匯出成的一個 JSON 檔，用來換裝置或留存。匯回來時是整份覆蓋，不與現有資料合併——原本有幾本、每本裝了什麼，全部換成備份裡的樣子。覆蓋的範圍包含這份資料的新舊：匯入一份舊備份，會讓這台裝置在雲端備份的新舊比較中變成較舊的一方。
_Avoid_: 存檔、快照、同步、匯出檔

**雲端備份**：
把備份改為自動存放在雲端，讓進度在使用者自己的多台裝置之間搬運。與備份同樣是整份覆蓋、不合併，差別只在自動且不經過檔案；覆蓋的內容一樣包含單字本與三組範圍選擇，因此在手機上設好的複習範圍，電腦打開也是同一組。兩邊內容不一致時，較新的那一份整份勝出，較舊的被蓋掉。
_Avoid_: 同步（會讓人誤以為兩邊的進度會被逐張合併）、雲端硬碟、帳號系統

**匯入單字**：
把一份備份檔裡的卡加進指定的一本，複習進度原封搬過來，已存在的詞跳過並在結束時一次列出。與「備份」的整份覆蓋是兩件事——它只往一本裡加東西，不動其他本，也不動現有的卡。
_Avoid_: 匯入（會與備份的匯入混淆）、同步、合併

**暱稱**：
使用者自取的名字，用來認出「這是同一個人的雲端備份」。不需註冊，但已經被別人用掉的暱稱無法佔用。
_Avoid_: 帳號、使用者名稱、識別碼

**密碼**：
使用者自取的一串字，是雲端備份的唯一保護。它同時決定誰能覆蓋這份備份、以及備份的內容怎麼被加密，因此遺失後無法補救。
_Avoid_: 通行碼、金鑰、密語

**指紋**：
由密碼算出、隨雲端備份一起存放的一小段值。用來判斷來寫入的人是否握有正確的密碼，本身無法反推出密碼。
_Avoid_: 雜湊、憑證、權杖、token
