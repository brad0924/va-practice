# va-practice

JLPT 單字複習工具。純前端閃卡應用，資料來源是 AnkiApp 匯出的 JLPT N2 牌組。

## Agent skills

### Issue tracker

Issue 與 spec 以 markdown 檔案存放在 `.scratch/<feature>/`，跟著版控走。See `docs/agents/issue-tracker.md`.

### Triage labels

沿用五個預設角色標籤，未做改名。See `docs/agents/triage-labels.md`.

### Domain docs

單一 context：根目錄 `CONTEXT.md` 加上 `docs/adr/`。See `docs/agents/domain.md`.
